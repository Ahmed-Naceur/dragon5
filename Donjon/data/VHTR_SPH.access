#!/bin/sh
if [ $# = 0 ]
   then
   echo "usage: VHTR_SPH.access directory" 1>&2
   exit 1
fi
ln -s $1/data/VHTR_SPH_proc/Multicompo Multicompo
ln -s $1/data/VHTR_SPH_proc/flux flux
ls -l
echo "VHTR_SPH access script terminated"
